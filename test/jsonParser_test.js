import {expect} from 'chai'
import jsonParser from '../src/jsonParser.js'

describe('jsonParser', function(){
  it('should return the path of the specified item in the JSON', function(){
expect(jsonParser()).to.equal('')
  })
})
